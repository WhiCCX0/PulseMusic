<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="horizontal">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/slider_start_value_text"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        tools:text="0" />

    <com.google.android.material.slider.Slider
        android:id="@+id/value_slider"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        app:thumbColor="?attr/primaryColor"
        app:thumbRadius="6dp"
        app:trackColorActive="?attr/primaryColor"
        app:trackColorInactive="?attr/overlayColor"
        tools:value="50"
        tools:valueFrom="0"
        tools:valueTo="100" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/slider_end_value_text"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:gravity="end"
        tools:text="100" />
</LinearLayout>